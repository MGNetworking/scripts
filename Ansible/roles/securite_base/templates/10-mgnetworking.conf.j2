# {{ ansible_managed }} — rôle securite_base (décision 19).
# Le port n'est jamais fixé ici : ce fragment ne change pas la façon dont on
# joint la machine, seulement la façon dont on s'y authentifie.
PasswordAuthentication no
KbdInteractiveAuthentication no
PubkeyAuthentication yes
